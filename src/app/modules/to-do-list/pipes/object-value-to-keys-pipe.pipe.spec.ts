import { ObjectValueToKeysPipePipe } from './object-value-to-keys-pipe.pipe';

describe('ObjectValueToKeysPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ObjectValueToKeysPipePipe();
    expect(pipe).toBeTruthy();
  });
});
